The open port script is designed to scan for possible open ports without having to check them one by one. 
Example command: ./open_ports.sh 127.0.0.1 1 65535 This would scan all ports from 1 to 65535.
Closed ports will not be shown on screen — only the open ones will appear.
